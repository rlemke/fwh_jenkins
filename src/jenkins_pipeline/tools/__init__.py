"""Domain CLI tools for the jenkins example.

The submodules under ``_lib/`` hold the deterministic simulator
implementation that both the ``tools/*.py`` CLIs and the FFL handlers
(``jenkins_pipeline.handlers.shared.jenkins_utils``) call into.
"""
